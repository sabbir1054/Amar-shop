import React from 'react';
import Header from '../Header/Header';

const Order = () => {
    return (
        <div>
            <Header></Header>
                <h1>I am order page</h1>
        </div>
    );
};

export default Order;